import React from "react";

const ExpenseComponent = (props) => {
    return(
        <h1 className={"text-muted text-center"}>
            Expense will be captured here.
        </h1>
    )
};

export default ExpenseComponent;